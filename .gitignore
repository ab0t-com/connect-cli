# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# temp / logs / local
*.tmp
*.bak
*.log
*.orig

# install scratch (install.sh works in a mktemp dir; never committed)
/tmp-install/

# NOTE: release/ is intentionally TRACKED — the per-platform binaries,
# checksums.txt, and VERSION are the published download artifacts.
